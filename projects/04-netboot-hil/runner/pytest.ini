[pytest]
# JUnit XML because that is what every CI system reads, and because the
# twenty-boot evidence is a pile of these rather than a transcript.
addopts = -v --junitxml=results.xml
testpaths = .
# A run that reaches the board at all takes a minute; one that cannot will
# otherwise sit on a console read until somebody notices.
timeout = 600
