# A router forwards. Without this the nftables forward chain never sees a
# packet and the symptom is a client with an address, a default route and no
# reachability, which reads like a NAT fault and is not one.
net.ipv4.ip_forward = 1
