# The metrics directory. Created by tmpfiles rather than by the package so
# that it also exists on a read-only rootfs with /var on tmpfs, which is
# where Project 19 takes this image.
d /var/lib/lte 0755 root root -
