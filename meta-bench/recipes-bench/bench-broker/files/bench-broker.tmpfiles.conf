# The broker's private key lives here with the certificates it presents.
# 0750 on the directory and 0640 on the key, owned by the mosquitto user:
# the broker has to read it and nothing else on the board should.
d /etc/bench/pki 0750 root mosquitto -
