# /run/bench holds the generated hostapd configuration, which carries the
# passphrase, and the DHCP lease file. Mode 0750 rather than the usual
# 0755: the directory listing alone says what this board is doing, and the
# file inside it is 0600.
d /run/bench 0750 root root -
