# Options the later bench projects need, switched on once here.
#
# Each line has to earn its place: if no project in the book uses it, it
# does not belong in the fragment.

# GPIO character device. libgpiod v2 speaks only to this interface, and the
# deprecated v1 ioctls are off so that nothing in the image can quietly use
# the old sysfs-era path. (Projects 1, 4, 7)
CONFIG_GPIO_CDEV=y
# CONFIG_GPIO_CDEV_V1 is not set

# Industrial I/O, with buffering and triggers, for the sensor projects.
# (Projects 9, 13, 16)
CONFIG_IIO=y
CONFIG_IIO_BUFFER=y
CONFIG_IIO_KFIFO_BUF=y
CONFIG_IIO_TRIGGER=y
CONFIG_IIO_SYSFS_TRIGGER=y

# Raw SPI and I2C access from user space, for bring-up before a driver
# exists. Modules rather than built in: they are debugging tools, not part
# of the running system. (Projects 3, 6, 11)
CONFIG_SPI_SPIDEV=m
CONFIG_I2C_CHARDEV=m

# Tracing, for the latency measurements later in the book. Cheap when idle.
# (Projects 8, 15)
CONFIG_FTRACE=y
CONFIG_FUNCTION_TRACER=y
CONFIG_DYNAMIC_FTRACE=y
CONFIG_PREEMPT_TRACER=y

# CONFIG_PREEMPT_RT belongs to Project 8, which builds its own kernel; it is
# deliberately absent here so that this image stays comparable to stock.
